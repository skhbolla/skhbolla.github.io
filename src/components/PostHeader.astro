---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

interface Props {
  title: string;
  subtitle?: string;
  author?: string;
  date: Date;
  formattedDate: string;
  heroImage?: ImageMetadata;
  displayNumber?: string;
}

const { title, subtitle, author, date, formattedDate, heroImage, displayNumber } = Astro.props;
---

<header class="post-header">
  <div class="meta">
    {author && <span>{author}</span>}
    {author && <span class="bullet">•</span>}
    <time datetime={date.toISOString()}>{formattedDate}</time>
  </div>
  <h1>
    {displayNumber && <span class="post-number">{displayNumber}. </span>}
    {title}
  </h1>
  {subtitle && <h2 class="post-subtitle">{subtitle}</h2>}
  <div class="header-divider">╌╌╌╌</div>
  {
    heroImage && (
      <div class="hero-container">
        <Image
          src={heroImage}
          alt=""
          class="hero-image"
          loading="eager"
          fetchpriority="high"
        />
      </div>
    )
  }
</header>

<style>
  .post-header {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    margin-bottom: 0;
  }

  .meta {
    margin-top: 0;
    margin-bottom: var(--space-6);
    color: var(--color-text-muted);
    font-family: var(--font-label);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  .bullet {
    margin-inline: var(--space-2);
    color: var(--color-accent);
    opacity: 0.8;
  }

  h1 {
    margin-top: 0;
    margin-bottom: var(--space-3);
    font-size: var(--text-3xl);
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tight);
    font-weight: 500;
    overflow-wrap: break-word;
    hyphens: auto;
    max-width: 100%;
  }

  .post-number {
    font-variant-numeric: tabular-nums;
  }

  .post-subtitle {
    margin-top: 0;
    margin-bottom: 0;
    font-size: var(--text-sm);
    font-weight: 400;
    color: var(--color-text-muted);
    line-height: var(--leading-snug);
  }

  .header-divider {
    font-family: var(--font-label);
    color: var(--color-meta-divider);
    letter-spacing: var(--tracking-header-divider);
    margin-top: var(--space-3);
    margin-bottom: var(--space-6);
    opacity: 0.6;
  }

  .hero-container {
    display: flex;
    justify-content: center;
    margin-top: var(--space-6);
    margin-bottom: var(--space-6);
    width: 100%;
  }

  .hero-image {
    width: 100%;
    max-width: 100%;
    height: auto;
    object-fit: cover;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-paper);
    border: var(--border-thin) solid var(--color-card-border);
  }
</style>
